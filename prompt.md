# Task: Flask Project Manager Tutorial Verification

You are provided with a Flask-based project management API.
Use `.venv` as the Python environment.
Follow `README.md` exactly to provision the app and reproduce the tutorial steps.

## Goals
- Validate that onboarding instructions succeed as written.
- Run every tutorial example and confirm the behavior matches the documentation.
- Capture failing commands with full error traces.
- Produce fixes for any documentation or code issues uncovered.
- Provide test cases to judge if function works

## Expected Output

1. `defects.txt` – List every defect discovered with reproduction steps and error traces.
2. `corrected_readme.md` – Updated onboarding + tutorial guide with working commands and code.
3. `requirements.txt` - Add required the libraries based on the .venv
4. `setup.sh` - Bash file to setup the environment
5. `test_files` - Test files to see if each function goes well
6. `run_tests.sh` - Test command to run testcases with pytest, all test should be passed

Focus on the "Tutorials" (examples) and "Onboarding" (setup) flows highlighted in the documentation.
